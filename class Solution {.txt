class Solution {
    static int minRepeats(String s1, String s2) {
        // code here
        string rep  = A;
        int count =1;
        
        while(A.size() < B.size()){
            
            A += rep;
            count++;
            
        }
        
        if(A.find(B) != -1){
            return count;
        }
        A+= rep;
        count++;
        
        if(A.find(B) != -1) return count;
        
        return -1;
    }
};